# default settings for container runtimes
DATABASE_URL=sqlite:/db/container.db
FRONTEND_DIST_PATH=/dist #will need to change, ignore

PAGES_DIR=/content/md
IMAGES_DIR=/content/images
AUDIO_DIR=/content/audio
VIDEOS_DIR=/content/videos

FRONTEND_WEBHOOK_URL=http://chasqui-frontend:4000/build
PORT=3003
